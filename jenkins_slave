pipeline {
    agent any
    parameters {
        string(name: 'name',  description: 'Enter your name')
        string(name: 'ages',  description: 'Enter your ages')
    }
    stages {
        stage('Example') {
            steps {
                echo "Hello from Slave ${params.name} of ${params.ages}"
                 sh 'python myscript.py Jeeraphat --age 21'
            }
        }
    }
}
