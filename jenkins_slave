pipeline {
    agent any
    
    stages {
        stage('Example') {
            steps {
                echo "Hello from Slave ${params.name} of ${params.ages}"
                 sh 'python myscript.py Jeeraphat --age 21'
            }
        }
    }
}
